/* Back End Notes: Document all functions. To create a JSDoc, place a cursor on top of the function.
  Type /** and then press Enter.
*/

/**
 *
 * @param test
 * @returns {*}
 */
export function test(test) {
  return test;
}
